# Bridge-specific default-value sentinels for DefaultValueResolver.
# Format: <fully.qualified.Type>=<java source expression>
#
# Keys are the reference type's FQCN as it appears in source
# (no '/' separators). Values are the exact Java source the generator
# emits as the return statement. The generator ensures the expression
# compiles in the context of the emitted stub (all names must be
# fully qualified or resolvable from java.lang / java.util).
#
# Populated per-ecosystem as stub phases land. Empty for now.
