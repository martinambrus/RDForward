// @rdforward:preserve - hand-tuned facade, do not regenerate
package org.bukkit.event;

/**
 * Pre-Bukkit-1.x {@code Event.Type} enum. Removed from Bukkit years
 * ago (replaced by per-event {@code HandlerList}+{@code @EventHandler})
 * but legacy plugins (LogBlockQuestioner 0.02) still reference it.
 *
 * <p>Stub provides only the constants currently encountered. Add more
 * names as plugins surface them — no need to mirror the entire pre-1.x
 * enum.
 */
@SuppressWarnings({"unused"})
public enum Event$Type {
    PLAYER_COMMAND_PREPROCESS
}
