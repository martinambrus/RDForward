package io.papermc.paper.event.server;

import org.bukkit.event.Event;

/**
 * Stub — the bridge never fires this event. Provided so plugins that
 * {@code @EventHandler} against it compile; the handler is silently never
 * called.
 */
public class PaperServerListPingEvent extends Event {
}
