package net.kyori.adventure.text.format;

/**
 * Marker interface for Adventure colour types. {@link NamedTextColor}
 * implements it; the bridge does not carry arbitrary RGB colours, so no
 * other implementation is provided.
 */
public interface TextColor {
}
